import React from 'react'
import Balance from './Balance'

const Header = () => {
    return (
        <div className="header">
            <h2 className="title-header">Expense Tracker</h2>
            <Balance />
        </div>
    )
}

export default Header
